# error.run
个人项目，用于学习、练习和探索

## Poem

```
         尘

         风过
         无痕
         心留
         一声
```

## Write

```
         # 入门指南

         这是一个**简洁**的 Markdown 编辑器
         支持实时预览和导出功能

         > 提示：使用 Markdown 语法进行格式化

         `const greeting = "你好，世界！";`

         - 轻量快速
         - 无追踪无广告
         - 数据保存在本地
```

## Room

```
         # 实时协作

         这是一个由 Yjs 和 WebRTC 驱动的
         **协作编辑器**

         > 提示：在多个标签页中打开相同 URL 即可协作

         - 通过 WebRTC P2P 实现实时同步
         - 带 emoji 指示器的远程光标跟踪
         - 使用 CRDT 自动解决冲突
         - 无需注册，即时协作
```

## Jump

```
         # 像素跳跃冒险

         这是一款使用 HTML5 Canvas 构建的
         **复古风格平台跳跃游戏**

         > 提示：收集金币，躲避敌人！

         - 程序化生成的平台
         - 随进度递进的难度
         - 复古像素美术风格
         - 支持移动端触控操作
```
